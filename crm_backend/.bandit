# Bandit configuration
# Scan severity: MEDIUM and above (skips LOW noise)
# -ll flag in CI = report MEDIUM + HIGH

profile:
  exclude_dirs:
    - "apps/*/migrations"
    - "apps/*/tests"
  skips:
    - B101